{
  "name": "backend_retrodenda",
  "version": "1.0.0",
  "scripts": {
    "dev": "json-server --watch database.json --port 8000",
    "start": "json-server database.json --port $PORT"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
